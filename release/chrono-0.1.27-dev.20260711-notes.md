# Chrono 0.1.27-dev.20260711

- Restored server-card latency placement beside the status dot and keeps online latency green.
- Tuned CPU Usage rows so pill mode is compact, fills the graph width, and keeps row percentages close.
- Added a Settings toggle to switch CPU Usage back to the compact dot-style rows.
- Updated CPU Usage legend/stat indicators to pill markers with aligned labels and values.
- Made CPU Load graph lines distinct red, blue, and amber, and scaled the chart to observed load average instead of CPU core count.
- Kept server-detail section labels away from red-prone metric accents for system, systemd, and process headers.
