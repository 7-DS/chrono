# Chrono 0.1.26-dev.20260711

- Restored the server-card latency label with a smaller display size.
- Changed server-details CPU usage core rows to compact pill bars that shrink for higher core counts.
- Removed the graph marker from the CPU Cores stat and widened spacing across the CPU stat row.
- Added a Settings app accent color option for buttons, toggles, focused fields, and confirmation actions.
